// src/components/Header.tsx
import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="header">
      <nav className="navigation">
        {/* Thêm menu navigation ở đây */}
      </nav>
    </header>
  );
};

export default Header;